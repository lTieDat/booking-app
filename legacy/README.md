# Legacy Archive

This folder keeps the original CRA-era source tree outside the active runtime surface.

- The current application runs entirely from `src/`
- Archived files in `legacy/cra-app/` are not part of the Vite + TypeScript app
- Keeping the archive separate makes the review surface cleaner while preserving older reference material
